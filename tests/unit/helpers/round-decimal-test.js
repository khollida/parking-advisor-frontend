
import { roundDecimal } from 'parking-advisor/helpers/round-decimal';
import { module, test } from 'qunit';

module('Unit | Helper | round decimal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = roundDecimal([42]);
  assert.ok(result);
});

