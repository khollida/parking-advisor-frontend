<div class="container">
  <div class="row top-buffer">
    <div class="col-lg-8 col-lg-offset-2">
      <div class="text-center">
        <h1>Meter <small>{{meter}}</small></h1>
        <h1><small>{{ticketCount}}</small> Citations</h1>
        <h1>Total <small>${{total}}</small></h1>
        <button type="button" {{ action 'switchView' }} class="btn btn-primary btn-lg view-chg">Detailed View</button>
      </div>
    </div>
  </div>
</div>
